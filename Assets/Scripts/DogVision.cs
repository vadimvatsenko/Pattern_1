using System.Collections;
using System.Collections.Generic;
//using UnityEditor.UIElements;
using UnityEngine;

public class DogVision : MonoBehaviour
{
    /*private GameObject _curentHitObject;
    private float _circleRadius;
    private float _maxDistance;
    private TagField _playerTag;
    private Vector3 _origin;
    private Vector3 _direction;
    private float _currentHitDistance;
    private Dog _dogController;

    private void Start()
    {
        _dogController = GetComponent<Dog>();
        _circleRadius = 5f;
        _direction = _dogController._direction;
    }

    private void Update()
    {
        _origin = transform.position;
        //RaycastHit hit = Physics.SphereCast(_origin, _circleRadius, _direction)
    }

    private void OnDrawGizmos()
    {
        Gizmos.color = Color.red;
        Gizmos.DrawLine(_origin, _origin + _direction * _currentHitDistance);
    }*/
}
